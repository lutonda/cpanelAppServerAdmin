
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <link href="https://fonts.googleapis.com/css?family=Karla:400,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.materialdesignicons.com/4.8.95/css/materialdesignicons.min.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/css/pricing-plan.css">
    <link href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet" integrity="sha384-wvfXpqpZZVQGK6TAh5PVlGOfQNHSoD2xbE+QkPxCAFlNEevoEH3Sl0sibVcOQVnN" crossorigin="anonymous">
</head>
<body>
<main>
    <div class="container-fluid">
        <h5 class="text-center pricing-table-subtitle">PRICING PLAN</h5>
        <h1 class="text-center pricing-table-title">Pricing Table</h1>
        <div class="row">
            {% for i,plan in plans %}
            <div class="col-md-3">
                <div class="card pricing-card pricing-plan-{{ plan.name|lower }} {% if plan.name|lower=='professional' %}pricing-card-highlighted{% endif %}">
                    <div class="card-body">
                        <i class="mdi {{['','mdi-cube-outline','mdi-trophy','mdi-wallet-giftcard'][i]}} pricing-plan-icon"></i>
                        <p class="pricing-plan-title">{{ plan.name }}</p>
                        <h3 class="pricing-plan-cost ml-auto">${{ plan.price }}</h3>
                        <ul class="pricing-plan-features">
                            <li>{% if plan.invoices == 0 %} Unimited {% else %} {{ plan.invoices }} {% endif %} Facturas/mês</li>
                            <li>{% if plan.invoices == 0 %} Unimited {% else %} {{ plan.amount}}  {% endif %} AKZ Facturação/mês</li>
                            <li>{% if plan.wareHouses == 0 %} Unimited {% else %}  {{ plan.wareHouses }} {% endif %} Lojas</li>
                            <li>{% if plan.users == 0 %} Unimited {% else %}  {{ plan.users }}  {% endif %} Utilizadores</li>
                            <li>{% if plan.pos != 0 %} POS {% endif %} </li>
                            <li>{% if plan.accounting != 0 %} Contabilidade{% endif %} </li>
                            <li>Gestão de Stock</li>
                            <li>Relatórios</li>
                            <li> {{ plan.daylyBackUps }} BackUps</li>
                        </ul>
                        {% if plan.name|lower=='professional' %}
                        <a href="#!" class="btn pricing-plan-purchase-btn">Free</a>
                        {% else %}
                        <a href="#!" class="btn pricing-plan-purchase-btn">Purchase</a>
                            {% endif %}
                    </div>
                </div>
            </div>
            {%  endfor  %}

        </div>
    </div>
</main>
<script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>
</body>
</html>
